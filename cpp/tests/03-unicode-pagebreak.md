# Unicode in slovenščini

Šumniki: č, š, ž, Č, Š, Ž. Naslov: ARHIM d.o.o., Matična številka 3812162000.

Ostale pisave: Ελληνικά, Кириллица, 日本語, العربية. Simboli: → ← ↔ € £ ¥ ° ± × ÷ ≤ ≥ ≠ ∞.

Emoji: ✅ ⚠️ 📄 🔧

This paragraph exists to confirm that `SetConsoleOutputCP(CP_UTF8)` on the console
side and the UTF-8 byte pass-through in `jsonEscape` both leave multi-byte
sequences intact all the way into the PDF.

<!-- NEW PAGE -->

# Second page

The marker above is replaced with a `page-break-after:always` div, so this
heading must start on page 2 of the PDF.

| Znak | Koda |
|------|------|
| č | U+010D |
| š | U+0161 |
| ž | U+017E |

<!-- NEW PAGE -->

# Third page

Two page-break markers, so this file must produce exactly three pages.
